/**
 * ViPER4Web WASM Module Loader
 *
 * This is a placeholder file. The actual viper4web.js and viper4web.wasm files
 * are generated by the build-web.sh script using Emscripten to compile the
 * C++ source code.
 *
 * To build the actual WASM module:
 * 1. Install Emscripten SDK
 * 2. Source the emsdk environment: source emsdk_env.sh
 * 3. Run the build script: ./build-web.sh
 *
 * The build will output:
 * - viper4web.js (WASM module loader)
 * - viper4web.wasm (compiled WebAssembly binary)
 *
 * This placeholder provides the expected module interface for type checking.
 */

var Module = typeof Module != "undefined" ? Module : {};

// Placeholder export - replace with actual build output
export default function createModule(moduleOverrides) {
  return Promise.reject(new Error(
    "ViPER WASM module not built. Run ./build-web.sh to compile the WASM module."
  ));
}
